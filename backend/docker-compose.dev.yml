services:
  api:
    command: npm run dev
    ports:
      - "8000:8000"
    volumes:
      - .:/app
      - /app/node_modules

  worker:
    command: npm run worker
    volumes:
      - .:/app
      - /app/node_modules